// Reserved for Supabase Auth session-refresh middleware once real
// authentication replaces the demo-session pattern used throughout the
// self-service and Workforce App layers. Not wired into a root
// middleware.ts yet — there is no session to refresh today.
export {};
